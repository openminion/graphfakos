{
  "name": "graphfakos-viewer-assets",
  "private": true,
  "scripts": {
    "build": "esbuild src/renderer.js --bundle --minify --format=iife --global-name=GraphFakos3DBundle --outfile=../src/graphfakos/assets/renderer-3d.js",
    "fixtures": "../.venv/bin/python ../scripts/generate_benchmark_envelopes.py --out-dir fixtures",
    "test": "npm run fixtures && playwright test"
  },
  "dependencies": {
    "3d-force-graph": "1.80.0",
    "three": "0.185.1"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "1.56.1",
    "esbuild": "0.25.11"
  }
}
